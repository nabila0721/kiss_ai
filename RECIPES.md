
can you authenticate me with the learningsystems workspace on slack using the slack agent?


Can you use  PWD/src/kiss/agents/third_party_agents/cron_manager_daemon.py to create a job which will check every 3 seconds if there are latest unresponsed messages from ksen in the channel kisssorcar using the slack agent, then it will run the messages as tasks one-by-one in the order of arrival and respond with the result suitably formatted for slack. If the message from ksen is at the top level, then run it as a new chat. Otherwise, if the message is a reply to a response, then run it as a task in the existing chat.

can you authenticate me with the imessages agent?

can you send "Hello" to 5102893391?

